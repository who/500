# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Environment files
.env
.env.*
!.env.example

# Credentials and secrets
credentials.json
*_secret*
*.pem
*.key

# OS
.DS_Store
Thumbs.db

# Beads viewer local config
.bv/

# Bot-params tuner artifacts (fh-sja.4): local overlays + resumable run state.
# The active local overlay and any tuner state files stay out of git; a
# promoted overlay is surfaced deliberately by fh-sja.6, not auto-committed.
packages/bots/params/local.json
packages/bots/params/*-tuned.json
*.tuner-state.json

# Human-decision report (regenerated by `ortus human`)
HUMAN-TODO.md

# Build artifacts and logs
*.log
logs/

# Cache directories
.cache/
.pnpm-store/

# Playwright artifacts
test-results/
playwright-report/

# TypeScript incremental build info
*.tsbuildinfo

# Python / Node / Go / Rust common build outputs
__pycache__/
*.py[cod]
.venv/
node_modules/
dist/
build/
target/

# grind harness lock (local only)
.beads/*.flock
